name: quotenote
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  dio: ^5.3.2
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  provider: ^6.0.5
  shared_preferences: ^2.2.1
  uuid: ^3.0.7

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
